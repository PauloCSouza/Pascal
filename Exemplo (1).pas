Program Pzim ;
var
	i : Integer;
	num : Integer;
	nome: String;
 Begin
 	for i := 1 to 10 do
 	begin
 	     writeln('Souza');
 	     writeln(num);
 	     num := num+1;
 	end;
  
  for num := 1 to 5 do
  begin
      readln(nome);
      writeln(nome);
  end;
  //i : Indice
 End.
